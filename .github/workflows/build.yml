name: Compilar APK desde zip

on:
  push:
    paths:
      - 'mi-tiguan.zip'
      - '.github/workflows/**'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: unzip -q mi-tiguan.zip -d proyecto
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
      - name: Compilar APK
        working-directory: proyecto/mi-tiguan
        run: gradle assembleDebug --no-daemon --stacktrace
      - run: cp proyecto/mi-tiguan/app/build/outputs/apk/debug/app-debug.apk MiTiguan.apk
      - uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ github.run_number }}
          name: Mi Tiguan (compilación ${{ github.run_number }})
          files: MiTiguan.apk
      
